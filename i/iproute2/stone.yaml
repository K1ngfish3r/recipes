# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : iproute2
version     : "7.2.0"
release     : 16
homepage    : https://wiki.linuxfoundation.org/networking/iproute2
upstreams   :
    - https://mirrors.edge.kernel.org/pub/linux/utils/net/iproute2/iproute2-7.2.0.tar.gz : fad570d3b044715955f816422264897bdb1b315d2b41e42ffbbf0989c9b3f7cd
summary     : Utility collection to control networking and traffic
description : |
    Iproute2 is a collection of utilities for controlling TCP/IP networking and traffic control in Linux. It supersedes ifconfig and route commands.
license     :
    - GPL-2.0-only
builddeps   :
    - binary(bison)
    - binary(flex)
    - pkgconfig(libbpf)
    - pkgconfig(libcap)
    - pkgconfig(libmnl)
    - pkgconfig(libtirpc)
setup       : |
    %patch %(pkgdir)/fhs.patch

    %configure \
        --color=auto
build       : |
    %make HOSTCC=${CC}
install     : |
    SBINDIR=%(sbindir) %make_install
    %install_file %(pkgdir)/%(name).tmpfiles %(installroot)/%(tmpfilesdir)/%(name).conf

    # Empty
    rmdir -v %(installroot)%(libdir)/tc
